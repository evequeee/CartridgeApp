<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CartridgeApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label Text="Cartridge Roulette" 
               FontSize="28" 
               FontAttributes="Bold" 
               HorizontalOptions="Center" />

            <Button Text="Spin the Cylinder" 
                Command="{Binding SpinCommand}" 
                HorizontalOptions="Center" 
                Padding="20,10" />

            <CollectionView ItemsSource="{Binding Games}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,8" Padding="15" CornerRadius="10">
                        <VerticalStackLayout>
                            <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding Developer}" FontSize="14" TextColor="Gray" />
                            <Label Text="{Binding Status}" FontSize="12" Margin="0,5,0,0" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
